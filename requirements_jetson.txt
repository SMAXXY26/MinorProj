# requirements_jetson.txt
# =============================================================================
# Jetson Orin Nano (JetPack 6.x / CUDA 12.6) dependencies
# =============================================================================
#
# torch + torchvision + onnxruntime-gpu:
#   Install SEPARATELY from Jetson-AI-Lab repo (standard PyPI has no ARM64 CUDA wheels):
#   pip install --extra-index-url https://pypi.jetson-ai-lab.io/jp6/cu126 \
#       torch torchvision onnxruntime-gpu
#
# tensorrt: pre-installed by JetPack 6.x — do NOT pip install
# pycuda:   do NOT install — use TRT Python bindings (import tensorrt as trt) directly
# conda:    do NOT use — TRT does not work inside conda on Jetson
# =============================================================================

# Core ML
ultralytics>=8.3.0
supervision>=0.21.0
numpy>=1.26.0,<2.0.0
opencv-python-headless>=4.9.0

# GPS / serial
pyserial>=3.5
pynmea2>=1.19.0

# MAVLink telemetry (optional)
pymavlink>=2.4.41

# Alert pipeline
requests>=2.31.0

# Utilities
python-dotenv>=1.0.0
PyYAML>=6.0
